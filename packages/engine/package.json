{
  "name": "@portale/engine",
  "version": "0.0.0",
  "private": true,
  "description": "The DM contract. Rules, canon, dice and the model seam for the AI dungeon master.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
